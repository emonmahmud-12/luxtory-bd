# Luxtory BD — Real Admin V4 (Standalone Fixed)

Public website remains normal by default. Opening `#owner` switches to a true full-screen standalone Admin Panel.

The standalone fix targets the actual DOM structure (`#owner` is inside `#top`) so the public header, feed, sections and footer are hidden in Admin mode.
